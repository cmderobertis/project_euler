use lib::{self};

fn main() {

    //RUN ALL PROBLEMS
    //lib::run_all();

    //RUN CURRENT PROBLEM
    lib::run_current();

    //RUN SPECIFIC PROBLEM BY FUNCTION NAME
    //lib::run(&(lib::pe15 as fn() -> i64));

}